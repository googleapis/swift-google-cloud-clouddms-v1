// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation

/// The types of jobs that can be executed in the background.
public enum BackgroundJobType: Codable, Equatable, Sendable {
  /// Unspecified background job type.
  case unspecified
  /// Job to seed from the source database.
  case sourceSeed
  /// Job to convert the source database into a draft of the destination
  /// database.
  case convert
  /// Job to apply the draft tree onto the destination.
  case applyDestination
  /// Job to import and convert mapping rules from an external source such as an
  /// ora2pg config file.
  case importRulesFile
  /// Encodes an unknown integer value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownIntValue(Int)
  /// Encodes an unknown string value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownStringValue(String)

  public init() {
    self = .unspecified
  }

  /// Returns the integer value associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown string value, this returns `nil`.
  public var intValue: Int? {
    switch self {
    case .unspecified: return 0
    case .sourceSeed: return 1
    case .convert: return 2
    case .applyDestination: return 3
    case .importRulesFile: return 5
    case .unknownIntValue(let v): return v
    case .unknownStringValue: return nil
    }
  }

  /// Returns the string value (or name) associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
  public var stringValue: Swift.String? {
    switch self {
    case .unspecified: return "BACKGROUND_JOB_TYPE_UNSPECIFIED"
    case .sourceSeed: return "BACKGROUND_JOB_TYPE_SOURCE_SEED"
    case .convert: return "BACKGROUND_JOB_TYPE_CONVERT"
    case .applyDestination: return "BACKGROUND_JOB_TYPE_APPLY_DESTINATION"
    case .importRulesFile: return "BACKGROUND_JOB_TYPE_IMPORT_RULES_FILE"
    case .unknownIntValue: return nil
    case .unknownStringValue(let v): return v
    }
  }

  /// Initialize from a string value.
  ///
  /// If the value is unknown, this initializes to [`unknownStringValue`](doc:BackgroundJobType/unknownStringValue(_:)).
  public init(stringValue: Swift.String) {
    switch stringValue {
    case "BACKGROUND_JOB_TYPE_UNSPECIFIED": self = .unspecified
    case "BACKGROUND_JOB_TYPE_SOURCE_SEED": self = .sourceSeed
    case "BACKGROUND_JOB_TYPE_CONVERT": self = .convert
    case "BACKGROUND_JOB_TYPE_APPLY_DESTINATION": self = .applyDestination
    case "BACKGROUND_JOB_TYPE_IMPORT_RULES_FILE": self = .importRulesFile
    default: self = .unknownStringValue(stringValue)
    }
  }

  /// Initialize from an integer value.
  ///
  /// If the value is unknown, this initializes to [`unknownIntValue`](doc:BackgroundJobType/unknownIntValue(_:)).
  public init(intValue: Int) {
    switch intValue {
    case 0: self = .unspecified
    case 1: self = .sourceSeed
    case 2: self = .convert
    case 3: self = .applyDestination
    case 5: self = .importRulesFile
    default: self = .unknownIntValue(intValue)
    }
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.singleValueContainer()
    if let v = try? container.decode(Int.self) {
      self.init(intValue: v)
      return
    }
    if let s = try? container.decode(String.self) {
      if let v = Int(s) {
        self.init(intValue: v)
      } else {
        self.init(stringValue: s)
      }
      return
    }
    throw DecodingError.dataCorruptedError(
      in: container, debugDescription: "Expected enum value, must be integer or string.")
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.singleValueContainer()
    switch self {
    case .unspecified: return try container.encode(0)
    case .sourceSeed: return try container.encode(1)
    case .convert: return try container.encode(2)
    case .applyDestination: return try container.encode(3)
    case .importRulesFile: return try container.encode(5)
    case .unknownIntValue(let v): return try container.encode(v)
    case .unknownStringValue(let v): return try container.encode(v)
    }
  }
}
